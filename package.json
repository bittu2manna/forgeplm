{
  "name": "forgeplm",
  "version": "1.0.0",
  "private": true,
  "description": "ForgePLM prototype with session-based authentication and RBAC",
  "scripts": { "start": "node server.js", "test": "node --test server.test.js" },
  "engines": { "node": ">=18" }
}
